[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "skillflow"
description = "Lightweight lifecycle orchestration for independent Claude Code runs."
readme = "README.md"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = ["pyyaml>=6.0,<7"]

[project.scripts]
skillflow = "skillflow.cli:main"

[dependency-groups]
dev = ["pytest>=8.4", "ruff>=0.6"]

[tool.hatch.version]
path = "src/skillflow/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/skillflow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
    "acceptance: live Claude Code tests (need SKILLFLOW_ACCEPTANCE=1, the claude CLI, and API quota)",
]

[tool.ruff]
src = ["src", "tests"]
target-version = "py314"
# SkillFlow's durable Run artifacts are Markdown at the repo root; a source
# formatter must not have edit authority over them.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
